# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/
.vscode/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# --- Added (Firebase & Android signing hygiene) ---
# Do NOT commit local.properties (contains local SDK paths)
android/local.properties

# Gradle local cache directory
.gradle/

# Android signing keys & properties (add your actual release keystore outside repo)
android/app/key.properties
*.keystore
*.jks
*.pem

# Firebase CLI debug log (if generated)
firebase-debug.log

# Crash / stack trace logs
hs_err_pid*

*.jks
key.properties
**/*.keystore
**/*.jks

# Firebase Admin SDK service accounts (downloaded from Firebase Console)
**/serviceAccount*.json
**/*-adminsdk-*.json
**/*-firebase-adminsdk-*.json

# Environment files
.env
.env.*

# Apple / misc private keys
*.p12
*.p8

# Optional: CocoaPods (Flutter can regenerate; keep Podfile & Podfile.lock if you prefer reproducible builds)
ios/Pods/
macos/Pods/

# Firebase Android config
android/app/google-services.json
lib/firebase_options.dart

